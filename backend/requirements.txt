# Runtime dependencies — used by the deployed app (Vercel + Docker) AND local
# dev. Deliberately excludes scikit-learn/scipy and shap: the app serves native
# LightGBM/XGBoost Boosters (no sklearn needed) and falls back gracefully when
# shap is absent. This keeps the Vercel serverless bundle ~187MB, under the
# ~250MB limit. For training (ml/train.py) or real SHAP locally, install
# requirements-dev.txt instead. See docs/DEPLOYMENT.md.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-multipart>=0.0.9
joblib>=1.4
numpy>=1.26
xgboost>=2.1
lightgbm>=4.5
google-auth>=2.30
requests>=2.31
